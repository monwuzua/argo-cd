apiVersion: v1
kind: Service
metadata:
  name: {{ include "fingridmon-helm-chart.fullname" . }}
  labels:
    {{- include "fingridmon-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fingridmon-helm-chart.selectorLabels" . | nindent 4 }}
